# 性能记录

> 口径来自 `docs/contributing.md` §6：**固定工况**（固定初值、固定迭代数、程序化网格）、
> 线程数按 1/2/4/8/18 呈现、**不假设线性加速**（本机 20 逻辑核含 4 个 E-core）。
> 原始日志在 `build/_baseline/`（未纳入版本控制；`BASELINE.md` 是汇总）。
> 方案与分阶段验收见 `docs/parallel-refactor.md`。
>
> **本文（2026-09-22 起）记录两条可选的附加约束**：剪切（§11）与线性中点弯曲（§12），
> 两者**默认都是关闭的**（`SceneConfig::shearStiffness = 0` / `bendStiffness = 0`），
> 关闭时行为与改动前逐字相同。§13 是弯曲 stencil 的**采样变体**降本实测：
> **单向（rows/cols）有效，隔行采样（stride ≥ 2）被实测否掉（退化）**。
> §14 是一个**被否掉的并行化尝试**（惯性右端 `single → for` 实测无收益）与其方法论收获。
>
> ⚠️ **单位口径修正（2026-09-23）**：两版 A/B 脚本原先把"单子步(ms)"抓成了 `总耗时` 的
> **秒数**（差 3.33 倍），§12.4 与 §13 的绝对值已按此换算修正；**百分比不受影响**。
> 详见 §12.4 末尾的说明。

---

## 0. 机器与测量纪律

| 项 | 值 |
|---|---|
| CPU | i7-12700F：8 P-core（含超线程）+ 4 E-core = 20 逻辑核 |
| 默认线程数 | **4**（2026-09-21 标定，见 §6；原先为 `max(1, 20-2)` = 18，实测每个工况都最差） |
| 编译器 | MSVC 19.40（VS2022），Release，`/openmp`（**经典 vcomp 运行时**，不是 `/openmp:llvm` —— 后者实测更差，见 §6.1） |
| 基准程序 | `pd_bench`（无窗口，`stepOnce` 直接循环） |

**三条必须遵守的测量纪律**（都是踩过的坑）：

1. **只比较"两侧物理完全相同"的运行**。同一工况下两次运行的迭代数、末次残差、最终应变必须一致，
   差别才只来自实现。下文所有对照都满足这一点。
2. **旧/新二进制必须同一轮会话内交替、重复多次取最小值**。同工况重复运行时单子步耗时波动可达
   **19 %**，而并行阶段（局部步、散射）可达 **1.5–2.1×**（单线程的全局回代稳定在 ±3 %）。
   单次运行的对照能给出完全相反的结论（本轮就遇到过：一次测得 Phase 2 慢 35 %，
   重复取最小值后是快 9 %）。
3. **分阶段耗时只作定性参考**：它同样受负载影响，占比的波动约 ±5 %。

---

## 1. Phase 0：改造前的基线（问题 5 的定量）

工况 A：`--grid 40 40 --steps 300 --iters 40 --residual-tol 0.3`（1600 顶点 / 3120 约束 / 12,000 次迭代）

| 阶段 | 1 线程 | 8 线程 | 18 线程 |
|---|---|---|---|
| 总耗时 | 1.891 s | 2.351 s | 4.160 s |
| 单子步 | 6.302 ms | 7.836 ms | 13.865 ms |
| 局部步 | 93.0 ms | 142.1 ms | **542.3 ms** |
| 散射 | 67.9 ms | 339.9 ms | **1110.1 ms** |
| 全局回代（串行） | 1294.2 ms | 1367.7 ms | 1797.8 ms |
| 其它（含未计时的残差） | 428.7 ms | 494.5 ms | 702.4 ms |

18 线程比 1 线程**慢 2.01×**，多出来的 2.27 s 按来源分解：

| 来源 | 占比 | 量级 | 证据 |
|---|---|---|---|
| **① 每次调用的区域开关（fork/join）** | 15.6 % | 10–25 µs/次（18 线程） | 局部步的超额不随工作量增长：40×40 **+24.1 µs/次**、100×100 +5.2、200×200 +14.0 |
| **② 散射归约的内存流量 O(P·dim)** | 46.8 % | 40×40 **1.38 MB/次** | 流量模型对三个规模误差 <3 %：69 vs 72.5 µs、430 vs 429 µs、2880 vs 2848 µs；搬运/有效 ≈ **P/2 ≈ 9×** |
| **③ 线程池活跃期间串行段被拖慢** | 25.4 % | 回代 1.12×（8T）/ 1.38×（18T） | 随线程数单调；只配置线程、从不进区域时 ≈0（10×10 回退串行：0.487 vs 0.520 s） |
| **④ 非线性残差（串行、无计时）** | 12.3 % | 0.021 ms/迭代（1T）/ 0.034（18T） | 判据开/关两次运行之差：656.6 − 238.0 = 419 ms |

三个"分离实验"的结论（`build/_baseline/e1_*.txt`、`e2_*.txt`、`e3_*.txt`）：

- **"开启并行"几乎不花钱**：建池一次性 **1–4 ms**（对总耗时做线性外推得到的截距差）；
  只配置线程但从不进入区域 ≈ 0。
- **"切换"有固定单价**：每次进入/退出区域 ~10–25 µs，工作量大就被摊薄
  （局部步 40×40 4.2× → 100×100 1.10× → 200×200 1.05×）。
- **"归约流量"与切换无关**：即使一个子步只开一次区域，这 1.38 MB 也得搬；它随 `P·dim` 线性增长。

---

## 2. Phase 1：一个子步一个区域 + 逐顶点融合（已完成 2026-09-21）

改动：`#pragma omp parallel` 从 `project` / `scatterInto` **函数内部**提到 `stepOnce` 最外层
（一个子步只进/出一次）；区域内的 `omp for` / `single` 取代 fork/join；
预测（+保存上一步位置 +初始化 previous）、解包（+收敛扫描 +previous）各自融合成一趟；
解包+判据成为独立的计时桶。

### 2.1 工况 A 的前后对照（同一轮会话，背靠背）

| 量 | 旧 1T | 新 1T | 旧 8T | 新 8T | 旧 18T | 新 18T | 18T 变化 |
|---|---|---|---|---|---|---|---|
| 总耗时 | 1.891 s | 1.857 s | 2.351 s | 2.115 s | 4.160 s | **3.296 s** | **−20.8 %** |
| 单子步 | 6.302 ms | 6.188 ms | 7.836 ms | 7.051 ms | 13.865 ms | **10.986 ms** | −20.8 % |
| 局部步 | 93.0 | 99.4 | 142.1 | 96.9 | **542.3** | **77.9** | **−85.6 %** |
| 散射 | 67.9 | 76.7 | 339.9 | 253.3 | 1110.1 | 797.6 | −28.2 % |
| 全局回代 | 1294.2 | 1277.4 | 1367.7 | 1376.0 | 1797.8 | 1611.7 | −10.4 % |
| 其它 | 428.7 | 8.4 | 494.5 | 20.0 | 702.4 | 172.8 | −75.4 % |
| （新）解包+判据 | — | 388.3 | — | 363.8 | — | 626.6 | — |

**要点**：局部步从"18 线程比 1 线程慢 5.5×"变成"比 1 线程**快** 1.28×" ——
fork/join 换成区域内的 barrier 之后，这笔钱基本收回来了（542 → 78 ms）。

### 2.2 其它工况（同一轮会话）

| 工况 | 旧 | 新 | 变化 |
|---|---|---|---|
| 40×40 / iters 10 / 18T | 2.995 ms | 2.614 ms | −12.7 % |
| 40×40 / iters 40 / 8T | 7.836 ms | 7.051 ms | −10.0 % |
| 10×10 / 2000 子步 / 18T（小网格，走串行回退） | 0.268 ms | 0.249 ms | −7.1 % |
| 100×100 / 20 子步 / 1T | 18.377 ms | 15.858 ms | −13.7 % |
| 100×100 / 20 子步 / 18T | 26.640 ms | 20.069 ms | −24.7 % |
| 200×200 / 8 子步 / 18T | 159.522 ms | 157.875 ms | −1.0 % |

规模越大收益越小，符合预期：大网格上区域开关本来就已被摊薄，剩余成本是散射流量与串行回代。

### 2.3 正确性与确定性（Phase 1 的验收）

| 检查 | 结果 |
|---|---|
| `pd_check` 8/8、三套测试 259 断言、`pd_trans` / `pd_chain` / `pd_diraudit` | 全部通过（exit 0） |
| **1 线程下新旧实现输出逐字相同**（含最终应变、能量、收敛统计、末次残差） | ✅ 相同 |
| 1 线程 vs 18 线程（打印精度内） | ✅ 一致 |
| 18 线程重复运行 | ✅ 逐字相同 |

---

## 3. Phase 2：边着色替代散射归约（已完成 2026-09-21）

改动：新增 `core/mesh/ConstraintColoring.h/.cpp`（并行的 Jones–Plassmann 风格着色 +
确定性哈希权重，随 `Mesh::buildSparsityPattern()` 一并构建）；散射不再用"每线程私有全维缓冲
+ 全量归约"，改为**按颜色分组执行** —— 同色边互不相邻 ⇒ 无冲突、无归约。
求和顺序全项目统一为**颜色序**（同一顶点在每一色里最多被写一次）。

### 3.1 着色结果（一次性、拓扑级）

| 网格 | 边数 | Δ | 颜色数 | 说明 |
|---|---|---|---|---|
| 2×2 | 4 | 2 | 2 | 最优 |
| 5×5 | 40 | 4 | 5 | |
| 20×20 | 760 | 4 | 6 | |
| 40×40 | 3120 | 4 | **6** | 上界 Δ+1 = 7；理论最少 4 |

> **纯极大独立集迭代（每轮一种新颜色）会给 15 色** —— 每次散射的颜色轮数 = 颜色数，
> 每色一个 barrier，15 色会把省下的流量全吃回去。加入"取最小可用颜色"（Jones–Plassmann）
> 后降到 6 色，这是本阶段实现里的关键决定之一。
>
> **一次性成本**：200×200（40k 顶点 / 79.6k 边）实测启动固定开销 Phase 1 `0.1554 s`
> → Phase 2 `0.1559 s`，即 **≈0.5 ms**，可忽略（每次散射省下的搬运量是 MB 级）。

### 3.2 前后对照（同一轮会话、交替重复 3 次取最小值）

| 工况 | Phase 1 ms/子步 | Phase 2 ms/子步 | 变化 |
|---|---|---|---|
| 40×40 / iters 40 / 18T | 9.738 | **8.848** | **−9 %** |
| 40×40 / iters 40 / 1T | 6.001 | 6.123 | +2 %（见下） |
| 100×100 / iters 10 / 18T | 22.440 | **18.277** | **−19 %** |
| 200×200 / iters 10 / 18T | 142.357 | **124.575** | **−13 %** |
| 200×200 / iters 4 / 8T | 51.261 | 48.734 | −5 % |

- **1 线程 +2 %** 是"统一求和顺序"的代价：着色路径多了一趟种子（`b = base`）与按颜色的循环，
  换来的是 1 线程与 18 线程**位级相同**（§3.4）。
- 散射阶段本身（单次运行的分段数据，只作定性）：40×40/18T 由 Phase 1 的 ~700 ms/300 子步
  降到 **~430–610 ms**；200×200/18T 由 202.6 ms 降到 **40.3 ms（5.0×）**。

### 3.3 踩到的坑：按边序分块会让**假共享**吃掉全部收益

第一版实现用 `schedule(dynamic, 64)` 按**边序号**分块，40×40/18T 的散射反而从
702.8 ms 涨到 **1312.9 ms（1.87× 更慢）**。

原因不是 barrier 数（每次散射 7 个 barrier，量级只有几十 µs），而是**假共享**：
同色边按边序号分块后，各线程写的顶点散布在整个 38 KB 的 `b` 上，
相邻顶点常共享同一条 64 字节 cache line ⇒ 该 line 在核心之间来回搬。

修法两条（都很便宜）：
1. **同色内按最小端点顶点排序**（构建着色时做一次）；
2. 散射的颜色循环改用默认的 **static（连续分块）**。

于是每个线程只写自己那一段**连续顶点区**，只剩区段边界有少量争抢；修完 1312.9 → ~432–610 ms。

> 教训：**并行写同一个大数组时，"写集合的空间局部性"与"有没有数据竞争"同样重要** ——
> 前者不会崩溃，只会悄悄慢 2–3 倍。

### 3.4 正确性与确定性（Phase 2 的验收）

| 检查 | 结果 |
|---|---|
| `pd_check` 8/8、`pd_trans` / `pd_chain` / `pd_diraudit` | 全过 |
| 三套测试 | **125 + 155 + 15 = 295 断言**，0 失败 |
| 着色恰当性（同色边不共享顶点）、完备性、CSR 自洽、颜色数 ≥ Δ | 全过（2×2/5×5/20×20/40×40） |
| 着色确定性（1 线程与 18 线程两次着色**逐位相同**；位形/刚度/pin 改变不影响） | 全过 |
| **散射跨线程数 + 重复运行位级一致** | **16/16 次位级相同**（Phase 1 时只有 2–3/16 次） |
| 并行 vs 边序参考（顺序已改为颜色序） | 最大差 1.14e-13，远低于 1e-12 判据 |

> **本阶段最硬的收益是"位级可复现"**：Phase 1 时 `schedule(dynamic)` 的分块分配让
> 求和分组随运行漂移，现在累加顺序固定为颜色序，断言已收紧为"必须位级相同"。

### 3.5 被否掉的假设：把散射也改成"逐顶点 gather"（负面结果，务必保留）

Phase 2 之后，散射每次仍有 **C 个 barrier**（40×40 时 C=6，加上种子趟共 7 次同步）。
一个自然的改进是**反过来做**：不要"逐边 scatter + 按颜色消冲突"，而是"**逐顶点 gather**"——
每个顶点读自己的关联边（CSR：`vertexStart[v]..vertexStart[v+1]`），只写自己的 3 个槽位。
这样一个 `omp for` + **1 个 barrier** 就够，颜色数、颜色轮、种子趟全都不需要了。

实现做完了（`ConstraintColoring` 里附带建 CSR 关联表，散射改成逐顶点 gather，
代价是每条边被两个端点各算一次投影），也在同一轮会话内交替测了 3 次取最小值：

| 工况 | 逐色 scatter | 逐顶点 gather | 变化 |
|---|---|---|---|
| 40×40 / iters 40 / 4T（默认） | 5.953 | 5.942 | 0 % |
| 40×40 / iters 40 / 18T | 8.154 | **7.620** | **+7 %** |
| 60×60 / iters 2 / 4T（交互档） | 0.780 | 0.786 | −1 % |
| 100×100 / iters 10 / 4T | 13.436 | 13.676 | −2 % |
| 200×200 / iters 10 / 4T | 116.778 | 116.949 | 0 % |
| 200×200 / iters 10 / 18T | 124.080 | 129.103 | **−4 %** |

**结论：在标定后的默认 4 线程下是平手（±2 %，噪声内）；只有"小网格 + 18 线程"这种
同步成本被放大到极致的组合才赢 7 %，而大网格 + 18 线程反而输 4 %**
（`b`/位置数组的遍历量翻倍 + 每顶点的 `isPinned` 查表重复，吃掉少数几个 barrier 的收益）。

更重要的推论是**预算已经不在散射上**（§5）：40×40/4T 下散射只占总时间的 **6–7 %**，
把它做到 0 也只能换来 6 %。因此**没有采纳** gather 散射，回退到已提交的逐色版本
（也是 GPU 方向更接近的写法）。这套 gather 实现没有白做 —— 它的正确用武之地是
**残差**（Phase 3，见 §7）：残差本来就是"每顶点一个量再取 max"的数据流，
gather 与它同形，且那里的 barrier 是**多出来的串行趟**换来的，不是平手。

> 教训：**瓶颈的归属要靠测量，不能靠推理**。改造前"散射有 7 个 barrier"看起来像是
> 明显的浪费，但在 4 线程下 barrier 的绝对成本只有几十 µs 量级，而散射的总时间
> 本来就只占 6 %；推理上"最优"的方案实测是平手。

---

## 4. 局部步的并行度与 M2 判据（重要结论）

`docs/plan.md` M2 的验收写着"**8 线程相对单线程在局部步上加速 ≥ 5×（N ≥ 40k）**"。
在 200×200（40,000 顶点 / 79,600 约束 / 400 次局部步）实测：

| 线程 | 局部步（400 次累计） | 相对 1 线程 | 单子步 |
|---|---|---|---|
| 1 | 194.873 ms | 1.00× | 52.934 ms |
| 8 | **82.724 ms** | **2.36×** | 54.212 ms |
| 18 | **49.376 ms** | **3.95×** | 64.370 ms |

**2.36× 未达 ≥5×，而且大概率是物理上限**：局部步每次调用要读写约 8.3 MB
（79,600 条边 ×（读两端位置 48 B + 读边 32 B + 写投影 24 B））。
1 线程 487 µs/次 ⇒ 约 **17 GB/s**（已是单核带宽极限）；8 线程 207 µs/次 ⇒ 约 **40 GB/s**，
约为该平台理论峰值（DDR4-3200 双通道 ≈ 51 GB/s）的 78 %。要 5× 需要 ~85 GB/s，**超出硬件能力**。

结论：**M2 的"≥5×"判据应改写**（例如改成"达到单核带宽的 ≥80 %"或给出带宽效率），
或者把局部步改成访存更省的布局。这条与 `docs/plan.md` §6 里"不承诺 20×"的注记同源，
现在有了量化依据。

---

## 5. 剩余的浪费构成（Phase 4 的目标）

工况 A、18 线程 vs 1 线程（**一次运行的分段数据**，占比有 ±5 % 波动）。Phase 2 之后
两边的差距已从最初 2.27 s 收到 **约 0.85–1.1 s**：

| 来源 | 量（18T − 1T） | 占比 | 归属 |
|---|---|---|---|
| 散射的颜色轮 barrier + 残余的写争抢 | +509 ms | 46 % | 还能再压：颜色数 6 → 更少、或把种子趟并进第一色 |
| 全局回代被线程池拖慢 | +239 ms | 22 % | **Phase 4**（换运行时 / 线程数策略 / 并行求解器） |
| 解包+判据（含残差与 barrier） | +215 ms | 19 % | ✅ **Phase 3 已完成**（2026-09-22，见 §7）：18T 上这一桶 469 → 140 ms |
| 区域开关 + 其它 | +159 ms | 14 % | 已大幅下降 |

**当前进度**：18 线程 vs 1 线程的比值 —— 改造前 **2.01×** → Phase 1 **1.78×** → Phase 2 **1.46×**；
标定后的默认 4 线程下，**并行已经胜过串行**（Phase 2 之后 4T vs 1T = −2.7 %…−9.0 %，见 §6），
Phase 3 又把这个差距推到 **−6.4 %…−11.3 %**（§7.2）。

**Phase 3 之后的构成（40×40 / 300 子步 / iters 40 / **4T**，最终二进制的阶段表）**：
全局回代 **76.3 %**、解包+判据 10.1 %、散射 6.5 %、局部步 5.3 %、其它 1.8 %（预测 0.1 + 组装 0.2 + 速度 0.0 + 其它 1.5）。
也就是说 —— **瓶颈只剩全局回代（Eigen 的稀疏三角回代，单线程）**。
Amdahl 上界很清楚：即使把回代之外的一切都并行到 0，也只能再拿掉 ~24 %；
要真正再上一个台阶，必须动求解器本身（Phase 4 的范围）。

> **动求解器的可行性已经量过了** —— 见 `docs/solver-feasibility.md`（2026-09-22，工具 `pd_solveaudit`）：
> 回代是**延迟受限**（达成带宽只有流式读写的 9–13 %）；层调度的并行回代原型实测只值 0.81×/1.45×/1.39×
> （40×40 / 100×100 / 200×200，相对 Eigen 串行），而**手写的串行回代内核（索引序）比 Eigen 的
> `solve` 快 19–29 %** 且不需要任何并发 —— 那才是这里性价比最高的一项。

> **注意本节表格里的 18 线程数字是"旧默认"下的历史记录**，其价值在于说明"线程开太多会倒退"；
> 当前默认是 4 线程（§6），引用性能时必须写明线程数。

---

## 6. 线程数标定：默认从 18 改成 4（2026-09-21）

原先默认是 `hardware_concurrency() - 2`（本机 18），理由是"留余量给 E-core"。
实测下来它是**每个工况下最差的**（同一轮会话内交替重复 3 次取最小值，vcomp）：

| 工况 | 1T | 2T | 4T | 8T | 18T |
|---|---|---|---|---|---|
| 40×40 / iters 40 / 300 子步 | 6.107 | 5.972 | **5.942** | 6.613 | 8.202 |
| 100×100 / iters 10 | 14.429 | 13.437 | **13.132** | 14.329 | 16.528 |
| 200×200 / iters 10 | 110.127 | **104.636** | 105.141 | 105.555 | 116.276 |
| 60×60 / iters 2（交互档） | 0.824 | 0.779 | **0.765** | 0.827 | 0.979 |

（单位 ms/子步；**加粗**为该行最优）

**三条原因**（互相独立，都在前面几节有量化）：
① 同步成本随参与者增多而上升 —— 本机是 8 P-core（含超线程）+ 4 E-core 的**异构**布局，
barrier 要等最慢的那条线程，线程还会跨核迁移；
② **串行段自己变慢**：Eigen 回代占 50–70 %，池在旁边时它变慢（回代 1219 → 1513 ms，1T → 18T）；
③ 每线程分到的活太小（40×40 每个颜色轮只有约 520 条边），摊不平同步成本。

**② 的机制单独做过实验（2026-09-22，回答"为什么线程开多了会拖慢串行段"）**：
"全局回代"写在 `omp single` 里 —— **只有一条线程干活，其余全在 barrier 上等**。这些等待的线程
默认是**自旋**等（占着执行单元、并持续访问共享的同步变量），于是那条唯一干活的线程被拖慢。
把 `OMP_WAIT_POLICY` 从默认改成 `passive`（真去睡）再测同一个工况：

| 200×200 / iters 10 / 18 线程 | 默认（自旋） | `OMP_WAIT_POLICY=passive` | `OMP_WAIT_POLICY=active` |
|---|---|---|---|
| 全局回代（ms/子步） | 86.8 | **82.0（−5.5 %）** | 90.2（+4 %） |
| 单子步 | 109.3 | **101.7（−7 %）** | 115.8（+6 %） |

同一份工作、同一个内核，**只是"等待的线程怎么等"变了** ⇒ 串行段的损失确实来自
"旁边线程抢资源"，不是算法本身。但它**不是免费午餐**：40×40/iters40（12,000 次散射、每次 7 个
barrier）上 `passive` 会让散射 **247 → 1999 ms**（每次 barrier 都变成唤醒线程），单子步 6.8 → 15.3 ms。
所以正确的做法仍是**少用线程**（默认 4），而不是靠运行时开关去救。

**结论**：默认改为 `min(hw - 2, 4)`。这是**机器相关参数**（与刚度的处理方式一致），
换机器或换到 ≫10 万约束的量级都要重新标定；运行时仍可用 `--threads` / `setNumThreads()` 覆盖。

**收益（新默认 4 线程 vs 旧默认 18 线程，同轮交替取最小）**：

| 工况 | 默认（4T） | `--threads 18` | 改善 |
|---|---|---|---|
| 40×40 / iters 40 | 6.016 ms | 8.139 ms | **−26 %** |
| 100×100 / iters 10 | 13.697 ms | 16.866 ms | **−19 %** |
| 200×200 / iters 10 | 104.775 ms | 113.778 ms | **−8 %** |
| 60×60 / iters 2（交互档） | 0.791 ms | 0.985 ms | **−20 %** |

**交互窗口实测**（`pd_viewer --frames 400`，60×60）：稳态 **60 FPS、物理 0.99–1.06 ms/帧**
（旧默认 1.2–1.3 ms；Phase 1 之前 1.9 ms）；启动瞬态 25–30 ms/帧（Phase 1 之前 36–52 ms）。
**Phase 3 之后**同一命令：稳态 60 FPS、物理 **0.90 ms/帧**、稳态迭代 **1 次**（残差 2.90 < 判据 2.943），
瞬态 23–27 ms/帧。

**对 Phase 2（着色）的影响要说清楚**：在 18 线程下着色带来 9–19 % 的提升，
但在**标定后的 4 线程**下它基本是平手（40×40 +5 %、100×100 −2 %、200×200 +1 %）——
因为它换来的主要是"归约在多线程下的惩罚"，而少用线程本身就消掉了那部分。
保留它的理由是另外两条：**位级确定性**（跨线程与重复运行 16/16 位级相同）与
**消除 O(P·dim) 流量**（线程数或规模上去以后会重新变成瓶颈）。

### 6.1 被否掉的假设：换 OpenMP 运行时（`/openmp:llvm`）

假设是"vcomp 让空闲线程自旋、拖慢串行段，换 libomp + `OMP_WAIT_POLICY=passive` 能救回来"。
实测**否掉**（40×40 / iters40 / 300 子步）：

| 运行时 | 1T | 4T | 18T | 18T 的散射阶段 |
|---|---|---|---|---|
| vcomp（默认） | 6.06 | 6.01 | **8.34** | 323 ms |
| libomp（`/openmp:llvm`） | — | 8.36 | **22.55** | **3466 ms（10.7×）** |
| libomp + `OMP_WAIT_POLICY=passive` | — | — | 21.41 | 3247 ms |

CMake 开关 `PD_OPENMP_LLVM` 保留（默认 OFF），以便复现这个负面结果。

### 6.2 测量事故与纪律（务必照做）

切回 vcomp 时，CMake 缓存里的 `OpenMP_CXX_FLAGS` 仍然是 `/openmp:llvm`（我写的 OFF 分支没有
`FORCE` 覆盖缓存），于是**有好几轮测量其实跑在 libomp 上**，得出过"4 线程最优、18 线程慢 39 %"
这类失真数字。暴露它的是巧合：40×40/18T 测到 **22.6 ms**，与 libomp 的 22.5 ms 几乎相同。

修法（已实施）：两个分支都 `FORCE` 覆盖；测量前核对**二进制实际链接的运行时**
（本文件用"在 exe 里数 `libomp` 字符串出现次数"的办法）与 `--threads` 是否真的生效；
每张表都写明运行时、线程数与工况。**任何"默认值"的改动都会让历史数字不可比**，
所以引用旧数字前先看它的条件。

---

## 7. Phase 3：残差并行化（已完成 2026-09-22）

### 7.1 改动

`nonlinearResidual` 原来是"区域内的 `omp single` 里跑一整趟串行 O(E)"：每个子步的**每次迭代**
都要串行遍历所有约束、重算投影、再遍历所有顶点取最大不平衡加速度。40 次迭代/子步时它是
仅次于全局回代的一块串行工作（§5 表里 +215 ms / 19 %）。

改成**逐顶点 gather + 与解包趟融合**：

| | 改造前 | 改造后 |
|---|---|---|
| 位置 | `single` 里独立一趟（含 barrier 等待） | 与"解包+收敛扫描+previous"**同一趟** `omp for` |
| 数据流 | 逐边累加进 `force[]`（scatter） | **逐顶点 gather**：读自己的关联边（CSR），只写自己的局部量 |
| 归约 | 串行 max | 每线程部分最大 + 固定顺序合并（与 diff/scale 同一套；MSVC 的经典 OpenMP 2.0 只认 `+ * - & ^ \|` 等，`reduction(max:)` 会直接报 C7660，要 `/openmp:llvm` 才认 —— 而换运行时已被否掉，见 §6.1） |
| 端点位置 | `m.positions`（解包之后） | **`xSolution`**（解包写的就是它 ⇒ 无读序依赖，因而能融合，不用多一次 barrier） |
| 投影次数 | 每条边一次 | **每个端点各一次**（gather 的固有代价，见下） |

关键点：**残差天生就是"每顶点一个量、再取 max"的数据流**，所以它适合 gather —— 这与散射
（§3.5 说 gather 在那儿只是平手）形成对照：散射写的是共享的 `b` 槽位，gather 化必须把逐边投影
改成逐顶点重算，多出来的投影没人替它摊，只能换到少几个 barrier。

**为什么单线程走另一条路（重要，别合并）**：串行时（`useParallel == false`，即单线程或小网格）
仍然用原来的边序串行版。原因是 gather 版每条边要让两个端点各算一次投影（多出来的 √、除法、
点积在串行时是纯亏），实测 40×40 / iters40 **6.010 → 6.403 ms/子步（+6.5 %）**。
两条路**不是两个定义**，而是同一口径的两个实现，且**数值逐位相同**：

- 关联表（CSR）是按**边号升序**填的（`buildAdjacency` 的 cursor 顺序遍历边表），
  所以"顶点 v 的关联边序"与"串行版按全局边序累加进 `force[v]`"是**同一个累加顺序**；
- 剩余运算（`inertia`、`|…|/m`、max）两边逐字同形。

因此串行时没必要付那 6.5 %。这条"逐位相同"的契约有运行时自查：`PD_DEBUG_RESIDUAL=1` 时
**两条口径都会算**并互相比较，相对差超过 1e-9 就打印告警（实测 0 次告警，差值恒为 0）。

### 7.2 前后对照（同一轮会话、交替 4 次取最小值，vcomp）

基线 `pd_bench_phase2.exe`（着色散射 + 串行残差）vs 新版（着色散射 + 并行残差）：

| 工况 | 基线 ms/子步 | 新版 ms/子步 | 变化 | 基线解包+判据 | 新版解包+判据 |
|---|---|---|---|---|---|
| 40×40 / iters 40 / **1T** | 5.983 | 5.982 | **0.0 %**（串行版，符合预期） | 373.8 | 376.2 |
| 40×40 / iters 40 / 4T（默认） | 5.851 | 5.479 | **−6.4 %** | 317.3 | **162.8** |
| 40×40 / iters 40 / 18T | 7.739 | 6.687 | **−13.6 %** | 469.2 | **139.8** |
| 60×60 / iters 2 / 4T（交互档） | 0.746 | 0.694 | **−7.0 %** | 209.0 | **102.0** |
| 100×100 / iters 10 / 4T | 13.864 | 12.291 | **−11.3 %** | 521.5 | **246.2** |
| 200×200 / iters 10 / 4T | 124.552 | 118.954 | **−4.5 %** | 1252.7 | **733.8** |
| 200×200 / iters 10 / 18T | 130.788 | 123.576 | **−5.5 %** | 1257.2 | **292.1** |

（"解包+判据"是该阶段 300（或 100）个子步的累计 ms，取各自最小那次运行的值；
散射阶段两边是同一份实现，表里未列，读数差在 ±5 % 分桶噪声内。）

**累计（40×40 / 300 子步 / iters 40）**：改造前 18T **13.865 ms** → Phase 1 10.986
→ Phase 2 8.848 → Phase 3 **6.687 ms（相对改造前 −51.8 %）**；而当前默认的 4T 是 **5.479 ms**，
比改造前的 18T 快 **2.53×**。

要读出的三件事：

1. **收益全部集中在"解包+判据"桶**（4T 上 −49 %、18T 上 −70 %…−77 %），也就是被串行趟 +
   barrier 等待占住的那段时间；其它阶段不变 —— 这正是"只改了谁在什么顺序里算什么"的证据。
2. **迭代数逐例完全相同**（40/40、2/2、10/10 …），因为残差两个口径逐位相同，放行判据不可能分叉。
3. **1 线程 0.0 %**：串行时走边序版，因此 Phase 3 的收益是纯粹的"并行收益"，
   不会通过拖慢 1 线程参照去虚增 `4T/1T` 的比值（§7.1 里那条"单线程走另一条路"的决定
   就是为了这一点：gather 版在 1 线程会白掉 6.5 %）。
4. 剩下没被吃掉的 200×200 只有 −4.5 %：那里的"解包+判据"仍占 734 ms（因为残差本身是
   O(E) 的真实工作，79,600 条边 × 10 迭代 × 100 子步 = 近 8000 万次投影重算），
   而 40×40/18T 的 −13.6 % 里有很大一块是**省掉的 barrier 等待**（469 → 140 ms）。

### 7.3 踩到的坑：热路径里加一段 O(1) 校验，散射 +28 %

关联表校验（"vertexStart/vertexEdges 规模与当前网格是否一致"）最初写在
`DistanceTerm::checkColoringContract` 里（和颜色表校验放一起最自然），而那个函数**每次散射
都被调用**。实测 40×40 / 300 子步 / 40 迭代（12,000 次调用）：

| | 1T 散射阶段 | 1T 单子步 | 4T 散射阶段 |
|---|---|---|---|
| 校验在散射里 | 128–133 ms | 6.20 ms | 108–111 ms |
| 校验挪到 `stepOnce`（一次/子步） | **100–102 ms** | 5.99 ms | 107–111 ms |

也就是说"几条指令"的 O(1) 校验带来了 **+28 %** 的散射耗时（1T 总时间 +4 %），
而 4T 完全看不到 —— 典型的**编译器代码布局被扰动**（增删这个函数里的代码会让 MSVC
重新安排热循环的布局/内联决策），不是指令数本身的代价。

处理：校验挪到 `Integrator::stepOnce` 里、**进并行区域之前、一次/子步**
（`checkAdjacencyContract`），保护不丢、热路径不被扰动。教训：
**"O(1) 所以放哪都行"是错的 —— 每次迭代都跑的代码，任何改动都要重新测量。**

### 7.4 正确性与确定性（Phase 3 的验收）

| 检查 | 结果 |
|---|---|
| `pd_check` 8/8、三套测试、`pd_trans` / `pd_chain` / `pd_diraudit` | 全过（**295 断言**，0 失败） |
| 两口径自查（`PD_DEBUG_RESIDUAL=1`，1T 与 4T 各 2 子步 × 8 迭代） | **0 次告警**（差值恒为 0，即逐位相同） |
| 迭代数（放行判据的行为）与基线逐例对比 | **完全相同**（40/40、2/2、10/10） |
| 关联表过期（改拓扑不重建）时的行为 | 进区域**之前**报错中止，不再变成越界读/卡住 |

---

## 8. Phase 4b：自己实现全局步回代（已完成 2026-09-22）

### 8.1 起因与改动

`docs/solver-feasibility.md` 的结论文档化了两个事实：回代是**延迟受限**的（达成带宽只有流式读写的
9–13 %）、而手写的串行回代比 Eigen 的 `SimplicialLDLT::solve()` 快 19–29 %（工具 `pd_solveaudit` 里
的层调度并行原型反而只值 0.81×/1.45×/1.39×）。于是**只做串行那一项**：

`core/solver/EigenDirectSolver.cpp` 现在接管三件事（分解本身仍是 Eigen 的 LDLᵀ）：

| | 原来（`EigenDirectSolver` 委托给 Eigen） | 现在（自己实现） |
|---|---|---|
| 排序 | Eigen 内部调 AMD（置换 `m_P` 是 protected，拿不到） | 自己调 `Eigen::AMDOrdering` + `NaturalOrdering` 的 LDLᵀ 分解置换后的矩阵，**置换与因子都在自己手里** |
| 置换 | `dest = m_P*b` … `dest = m_P⁻¹*dest` | 同两步，但省掉 `PermutationMatrix` 表达式的中间对象 |
| 对角缩放 | `m_diag.asDiagonal().inverse()` —— **每次 solve 重做 n 次除法** | 数值分解后预存 `1/D`，每次只乘 |
| 前代 | `matrixL().solveInPlace()` | 自己扫 CSC 列（scatter 形式） |
| 回代 | `matrixU().solveInPlace()`，走**因子的转置视图** | 自己扫 CSC 列（作用相同，访存更直） |

两个必须记住的实现要点（都踩过，写进了源码注释）：

1. **因子的存储方向决定两个三角求解怎么写**。`L(i,j)` 在**第 j 列**，所以回代
   （`x[j] -= L(i,j)·x[i]`，i>j）扫列天然可用；而前代（`y[j] -= L(j,i)·y[i]`，i<j）如果照抄
   "扫第 j 列、按 i<j 筛"，会**一条都命不中**，等于把前代写成恒等式 `y = b`。
   症状是"解差 5 %、残差 5.9 而 Eigen 是 1e-12"。正确写法是 scatter：扫第 j 列时把 `y[j]`
   （此刻已是终值）推给所有 i>j。
2. **两种形式的累加顺序完全相同**，所以结果与 Eigen **逐位一致**（见 §8.3）。

### 8.2 前后对照（同一轮会话内交替 5 次取最小值，vcomp，4 线程）

| 工况 | 基线 ms/子步 | 新版 ms/子步 | **总变化** | 基线"全局回代" | 新版"全局回代" | 回代变化 |
|---|---|---|---|---|---|---|
| 40×40 / iters 40 / 300 子步 | 5.457 | **5.016** | **−8.1 %** | 1261 ms | 1129 ms | **−10.5 %** |
| 60×60 / iters 2（交互档） | 0.700 | **0.629** | **−10.1 %** | 532 ms | 466 ms | **−12.3 %** |
| 100×100 / iters 10 | 10.839 | **9.741** | **−10.1 %** | 1852 ms | 1633 ms | **−11.8 %** |
| 200×200 / iters 10 | 118.406 | **96.902** | **−18.2 %** | 7906 ms | 6283 ms | **−20.5 %** |

收益随规模变大（回代占比随规模上升），交互档也有 −10 %。验收：`pd_check` 8/8、**295 断言**、
`pd_trans`/`pd_chain`/`pd_diraudit` 全过。

**测量过程本身值得记一笔**：这次读数在会话内会**单调漂移**（同一个 200×200 基线二进制从 155 ms
一路降到 123 ms），所以"交替 + 取最小值"不是形式主义 —— 有一次没受控的对照给出过 −33.9 %，
按纪律重测后是 −18.2 %。
另外交互查看器自报的"物理 ms/帧"波动太大（同一改动前后测到 0.89 / 1.24 / 1.46 ms 三个值），
不适合用来验证 10 % 量级的变化；交互档的结论以上表 60×60/iters2 的基准运行为准。

### 8.3 最硬的性质：物理结果**逐位不变**

新实现与本改动前的二进制在 40×40 与 200×200 上跑同一批子步，**全部物理输出逐字相同**
（收敛统计、末次残差 8.63991 m/s²、最终应变、全程最大应变、弹性能/总能量）。
原因是两者的求和顺序在浮点上完全相同：每个节点的各项都按"对方的列号递增"逐个相减。
这条性质意味着：

- **不需要重新标定任何历史数字**（性能数字之外的物理基线、回归容差、查看器观感全都不变）；
- 这次改造是纯粹的"同样结果、更少时间"，没有引入任何数值口径变化。

### 8.4 试过但被否掉的变体：给前代另存一份 CSR

最早实现的是"因子另存一份 CSR（行存储），前代按行 gather"——在 `pd_solveaudit` 的
**热数据**紧循环里它比 Eigen 快 23–29 %，但在真实管线里（每次 solve 之间隔着 project / 清缓冲 /
scatter 对 1–40 MB 数据的访问，因子是**冷的**）它要多读一整份因子副本，收益被压到 −8…−10 %
（200×200 上 39 MB 的额外流量）。改成 scatter 形式（只读 CSC 因子，不建副本）后是 −18.2 %。
存档：`build/_baseline/pd_bench_a_csr.exe`（变体 A）。

> 教训：**微基准的热数据结论不能直接搬到管线里**；"少搬一份 39 MB"比"少几条指令"重要。

---

## 9. Phase 4c：全局步按 x/y/z 三分量并行（已完成 2026-09-22）

> 本轮把"回代能不能并行"这个问题**换了个问法**：不要去并行化三角求解**内部**（那条路 §8 之前的
> `docs/solver-feasibility.md` 已经量过：层调度只值 0.81×/1.45×/1.39×，因为层内工作量中位只有
> 288/492/948 项，与一次同步的成本同阶），而去问"**这个系统里是不是本来就有独立的右端项**"。
> 答案是：有，而且免费有 3 个。

### 9.1 新发现：`L = Ã ⊗ I₃`，3n 个方程本来是 3 个互不相干的标量系统

组装代码里**每一块都是标量 × I₃**（这不是巧合，是距离约束 + 各向同性质量 + pin 的必然结果）：

| 项 | 代码位置 | 形状 |
|---|---|---|
| 惯性 `M/h²` | `core/assemble/Assembler.cpp` L93–98 | `(3v+d, 3v+d)` 三个 d 同一个值 |
| 距离约束 `κ AᵀA` | `core/energy/DistanceTerm.cpp` L275–290 | `(3a+d, 3a+d)`、`(3a+d, 3b+d)`，**d 不变** |
| pin 行 `I` | `Assembler.cpp` L105–117 | `(3v+d, 3v+d) = 1`，三个 d 各一份 |

即 `L = Ã ⊗ I₃`（`Ã` 是 n×n 标量矩阵，行序是"顶点主序" 3i+0/3i+1/3i+2）。图论后果：
**L 的图是 3 个互不相连的连通分量**，而消元的填充**不可能跨连通分量** ⇒ 因子是 3 份结构同构的
标量因子。于是"一次 3n 三角求解"可以拆成 3 条**互不相干**的链。

为什么这件事正好解决瓶颈：§8 之前测到回代**达成带宽只有流式读写的 9–13 %**，也就是说缺的是
**未决访存请求数（MLP）**，不是搬运能力。3 条独立链分给 3 条线程，恰好把 MLP 提高约 3 倍
—— 而**不需要**三角求解内部的任何并行化、不需要任何同步。

**结构自检（工具 `pd_solvecomp`，`_verify/solve_components.cpp`）**：

| 规模 | 因子 nnz | 每个分量的列数 | 每个分量的因子 nnz | 跨分量的 i≠j 项 |
|---|---|---|---|---|
| 40×40 | 66,027 | 1600 / 1600 / 1600 | 22009 / 22009 / 22009 | **0** |
| 100×100 | 637,233 | 10000 ×3 | 212411 ×3 | **0** |
| 200×200 | 3,255,033 | 40000 ×3 | 1085011 ×3 | **0** |

### 9.2 改动

| 文件 | 改动 |
|---|---|
| `core/solver/IGlobalSolver.h` | 新增 `parallelComponents()`（默认 1）与 `solveComponent(c, b, x)`（默认转发给 `solve`）—— 结构不满足时自动退回整趟求解 |
| `core/solver/EigenDirectSolver.cpp` | `factorize()` 成功后做一次 O(nnz) 的**分量结构分析**（用 label 向量拿"置换后列号 → 原索引"，自检是置换、三个分量等大、填充不跨分量），建好每个分量的列号表；新增 `solveComponent()` |
| `core/assemble/Assembler.{h,cpp}` | 新增 `applyPinRhsComponent()`：只覆盖第 c 个分量的 pin 行（分量间下标不相交 ⇒ 可并发写同一向量） |
| `core/sim/Integrator.cpp` | 迭代里的 `omp single { applyPinRhs; solve }` → **`omp for schedule(static,1)`**（3 个迭代，每分量一趟）。结构不满足时循环只有 1 个迭代，行为与原来的 `single` 一致 |
| `app/bench/main.cpp` | "单次回代"改用 `累计/次数`（拆分后 `lastSolveSeconds` 会退化成单个分量的切片）；更新那条过期的注记 |

**零同步、零竞争**：分量之间的自由度集合不相交，所以 3 条线程写的是同一块缓冲的不同位置，
既不需要锁也不需要额外 barrier（`omp for` 的隐式 barrier 与原来 `single` 的隐式 barrier 数量相同）。

### 9.3 微基准：单次 solve（`pd_solvecomp`，同一并行区域内逐轮交替取最小）

| 规模 | 整趟（现状形状） | 三分量并行 | 加速 | 达成带宽（整趟 → 并行） |
|---|---|---|---|---|
| 40×40 / 4 线程 | 0.0789 ms | **0.0335 ms** | **2.36×** | 21 → 47 GB/s |
| 40×40 / 3 线程 | 0.0796 ms | **0.0348 ms** | 2.29× | — |
| 40×40 / 18 线程 | 0.0883 ms | 0.0427 ms | 2.07× | — |
| 100×100 / 4 线程 | 0.7137 ms | **0.2511 ms** | **2.84×** | 21 → 61 GB/s |
| 100×100 / 18 线程 | 0.8054 ms | 0.3239 ms | 2.49× | — |
| 200×200 / 4 线程 | 4.6732 ms | **2.3223 ms** | **2.01×** | 17 → 34 GB/s |
| 200×200 / 18 线程 | 5.6917 ms | 2.4570 ms | 2.32× | — |

"按分量拆开但串行跑"（同工具里的 C 档）与整趟基本持平（40×40 上 1.04–1.07×、200×200 上 0.96×）
⇒ 收益来自**并行**，不是循环结构变化。`omp for` 构造本身的固定成本实测只有 **0.2–1.1 µs**。

### 9.4 生产前后对照（同一轮会话内交替 5 次取最小值，vcomp，vcomp 核对 libomp=0）

| 工况 | 基线 单子步 | 新版 单子步 | **总变化** | 基线"全局回代" | 新版"全局回代" | 回代变化 |
|---|---|---|---|---|---|---|
| 40×40 / iters 40 / 300 子步 / **4T** | 4.966 ms | **2.867 ms** | **−42.3 %** | 1106.6 ms | 500.0 ms | **−54.8 %** |
| 40×40 / iters 40 / 300 子步 / **1T** | 5.490 ms | 5.329 ms | −2.9 % | 1067.9 ms | 1013.8 ms | −5.1 % |
| 40×40 / iters 40 / 300 子步 / **18T** | 6.603 ms | **3.612 ms** | **−45.3 %** | 1463.3 ms | 626.0 ms | **−57.2 %** |
| 60×60 / iters 2（交互档）/ 4T | 0.637 ms | **0.377 ms** | **−40.8 %** | 137.8 ms | 56.4 ms | −59.1 % |
| 100×100 / iters 10 / 4T | 10.123 ms | **4.764 ms** | **−52.9 %** | 494.6 ms | 172.4 ms | **−65.1 %** |
| 200×200 / iters 10 / 4T（仅 12 子步） | 75.549 ms | 51.684 ms | **−31.6 %** | 677.9 ms | 379.7 ms | −44.0 % |

- 主工况从 **4.966 → 2.867 ms/子步**；相对本文件 §1 记录的改造前基线（18T 13.865 ms），累计 **4.8×**。
- **上表是第一轮测量**。看板用的是 `build/_perf/refresh_report.ps1` 生成的**第二轮**（同轮会话内交替 5 次
  取最小；与第一轮差 0.5 个百分点以内，属噪声）：40×40/4T **4.997 → 2.910（−41.8 %）**、
  18T 7.257 → 4.097（−43.5 %）、交互档 0.663 → 0.385（−41.9 %）、100×100 11.714 → 5.969（−49.0 %）、
  200×200 92.123 → 65.796（−28.6 %）。看板生成链：
  `.\build\_perf\refresh_report.ps1`（写 `build/_perf/logs/` 与 `build/_baseline/phase4c_ab.log`）
  → `node build\_perf\make_report.js` → `node build\_perf\serve.js`（http://127.0.0.1:8137/）。
- **补充的两组数字（同轮交替取最小）**：
  · 200×200 / iters10 / **100 子步**（原先只用 8–12 子步，一次性分解被摊入而偏保守）：
    旧 **107.64** → 新 **59.54 ms/子步** = **−44.7 %**；
  · 查看器（`pd_viewer --frames 400`，60×60）：稳态 **60 FPS、物理 0.55–0.59 ms/帧、迭代 1 次、
    残差 2.90**（Phase 4c 之前 0.90 ms/帧 ⇒ −39 %）；瞬态 11.9–13.1 ms/帧（原先 23–27）。
  · 线程数复测 4T vs 5T（40×40/iters40）：3.015 vs 3.052（+1.2 %）⇒ **默认 4 不动**。
- 200×200 只跑了 12 个子步 ⇒ 一次性"组装+分解"被摊进单子步，**这个数字偏保守**
  （同样的记账问题在 §5 关于看板的注里提过）。
- 1T 只有 −2.9 %：这条路径没有并行可拿，变化来自"按分量的列表"的局部性（solve 桶 −5.1 %）
  与桶口径变化；在微基准里它同样是平的（0.96–1.07×），属预期。

### 9.5 最硬的性质：**逐位不变**，而且与线程数无关

| 检查 | 结果 |
|---|---|
| 与改动前二进制逐字比对物理输出（40×40，1T / 4T / 18T） | **全部逐字相同**（收敛统计、末次残差 8.63991 m/s²、最终应变、全程最大应变、弹性能/总能量） |
| 跨线程数逐字一致（1/2/3/4/5/8/18 线程） | **全部逐字相同** |
| 回代次数计数 | 仍是 12,000（= 迭代数）—— `solveCalls` 只在分量 0 的调用上自增 |
| 新增回归断言 | `test_primitives` **+5 条**（`parallelComponents()==3`、分量解 == 整趟解**逐位**、pin 分量覆盖 == 整份覆盖 ×2）⇒ **130 + 155 + 15 = 300 断言** |

为什么必然逐位相同：每个分量的列顺序（升序/降序）、`i>j` 筛选、累加顺序都与整趟完全一致，
只是"别的分量的列"本来也不会写入本分量的行。这条性质是本次改造敢做的根本原因
—— 它意味着**不需要重新标定任何物理基线**。

### 9.6 两个必须记住的口径与局限

1. **"全局回代"桶的语义变了**：原来是 `omp single` 里"唯一执行者自己的执行时间"（不含 barrier
   等待），现在是 `omp for` 之后线程 0 记的**阶段墙钟**（含等最慢的那条线程），与散射/解包桶
   口径一致。引用 §8 之前的回代数字（76 % 占比等）时必须注意这个差别。
2. **这个并行只对"⊗ 结构"成立**：一旦引入非各向同性的能量项（各向异性弯曲、IPC 的 barrier
   Hessian、摩擦耦合等，块不再是标量 × I₃），填充就会跨"分量"，`EigenDirectSolver` 里的
   结构检查会命不中、`parallelComponents()` 自动退回 1 —— 功能不会错，但这条加速会消失。
   新加能量项时要**重新检查这条性质**（`pd_solvecomp` 的第一节会直接告诉你；
   完整的问题清单与后续机会见 `docs/open-issues.md`）。

---

## 10. Phase 4d：投影与散射融合（已完成 2026-09-22）

### 10.1 改动与"为什么必然逐位不变"

原来每迭代是两趟：`projectInRegion` 把每条边的投影 `d_c` 写进 `targets`（物化），
`scatterIntoInRegion` 再读 `targets` 累加到右端 `b`。现在合成**一趟**（按颜色就地算 `d_c`
直接累加），`targets` 不再物化：

```cpp
// core/energy/DistanceTerm.cpp
inline Vec3 projectEdgeValue(const Mesh&, const Edge&);                    // 投影（唯一实现）
inline void scatterEdgeValue(const Mesh&, const Edge&, const Vec3& d, Scalar* out);  // 散射（唯一实现）
// 三条使用路径：
//   projectEdge()          = targets[c] = projectEdgeValue(...)          （两趟路径）
//   scatterEdge()          = scatterEdgeValue(..., targets[c], ...)       （两趟路径）
//   projectAndScatter...() = scatterEdgeValue(..., projectEdgeValue(...), b) （融合路径）
```

**关键**：两条路共用**同一份**算术 helper ⇒ 每个操作、每个操作数顺序都逐字相同；
而每个顶点的累加顺序仍是颜色序（同色内每个顶点最多被写一次）⇒ **结果逐位相同**，
不是"在 1e-12 内相同"。所以这次改造同样是"零数值风险"：不需要重新标定任何物理基线。

省的账（40×40，每迭代）：`targets` 的写 + 读各 3120×24 B ≈ 75 KB、两个端点位置少读一遍；
另外 `targets`（75 KB）不再占用缓存。**种子趟 `b = bBase` 保留不动** —— 它只有 O(dim) 流量与
1 个 barrier，而"把首色并进基数写"需要在每条边的热循环里判"该顶点是否首次被触碰"，
按 §7.3 的教训（热循环里加一点点东西都可能被编译器布局放大）不划算。

### 10.2 前后对照（同轮会话内交替 5 次取最小值，4 线程，vcomp）

| 工况 | 基线 ms/子步 | 新版 ms/子步 | **总变化** | 基线"局部步+散射" | 新版"投影+散射" |
|---|---|---|---|---|---|
| 40×40 / iters 40 / 300 子步 | 3.081 | **2.656** | **−13.8 %** | 193 ms | **90.9 ms（−53 %）** |
| 40×40 / iters 40 · **18 线程** | 4.140 | **3.643** | **−12.0 %** | 365.1 ms | 240.4 ms |
| 60×60 / iters 2（交互档） | 0.389 | 0.358 | −8.0 % | 17.3 ms | 8.7 ms |
| 100×100 / iters 10 | 6.226 | 5.697 | −8.5 % | 14.1 ms | 6.5 ms |
| 200×200 / iters 10 | 67.333 | 65.239 | −3.1 % | 33 ms | 21.9 ms |

收益（−3…−14 %）远大于原先"粗估 3–8 %"的估计 —— 大头来自 `targets` 的写 + 读与位置数组的
重复读取（在 40×40 上 `targets` 是 75 KB，已经装不进 L1）。大规模上收益递减，因为那一档
主要是访存受限。

**阶段构成随之变化（40×40/iters40/4T，累计 ms/占比）**：全局回代 **528.6 / 65.4 %**、
解包+判据 170.3 / 21.1 %、**投影+散射 89.7 / 11.1 %**、其它 2.4 %。
⚠️ **桶口径**：`局部步` 与 `散射` 已合并为 `投影+散射`（单子步总耗时 2.695 ms）——
引用历史"局部步 10 % + 散射 11.7 %"时要记得把它们相加再比。

### 10.3 验收

| 检查 | 结果 |
|---|---|
| `pd_check` 8/8、**306 断言**、`pd_trans`/`pd_chain`/`pd_diraudit` | 全过 |
| 与融合前二进制逐字比对物理输出（40×40/iters40、60×60/iters2、100×100/iters10） | **全部逐字相同** |
| 独立回归断言 `fusedProjectScatterMatchesSplitPath`（融合路径 == 两趟路径，`memcmp` 逐位） | ✅ 已补（`test_primitives` 130 → **132** 断言） |

---

## 11. 剪切（对角）约束：可选，已完成 2026-09-22

**动机**：默认约束集只有 4-邻域距离约束 + pin，四边形可以自由塌成平行四边形 —— 布料没有面内抗剪。

**实现（刻意走"复用"路线）**：`Mesh::addShearDiagonals(nx, ny, stiffness)` 给每个四边形加**一条对角边**。
对角边就是一条普通的 `Edge`（距离约束），因此**装配、边着色、投影/散射、残差、残差融合、
以及 Phase 4c 的三分量并行统统不用改**；两条架构性质也不受影响：
① 左端矩阵仍与位形无关 ⇒ 只分解一次；② 每块仍是标量 × I₃ ⇒ `L = Ã ⊗ I₃` 保住。
函数**内部自动重建稀疏结构/着色/关联表** —— 2026-09-22 漏过一次，被残差的契约检查当场挡下
（`vertexEdges 6240 项（期望 9282）`），所以现在把它放进 API 里而不是留给调用方。

开启方式：`SceneConfig::shearStiffness > 0`，或环境变量 `PD_SHEAR=<刚度>`（便于 bench/viewer 做对照）。
**默认 0 = 关闭**，此时网格、边表、装配与之前**逐字相同**（已用 pre-fuse 二进制比对 `True`）。

**实测（40×40 / iters40 / 300 子步 / 4T，κ=1e4）**

| 量 | 关闭 | `PD_SHEAR=1000` | `PD_SHEAR=10000` |
|---|---|---|---|
| 约束数 | 3120 | 4641 | 4641 |
| 最终应变 max / mean | 0.0586 / 0.001350 | 0.0778 / 0.002158 | **0.0362 / 0.000766** |
| 弹性能 | 0.322 | 0.0872 | **0.0441** |
| 单子步 | 2.749 ms | 3.448 ms | **3.329 ms（+21 %）** |

- **物理效果**：剪切刚度越大，面内变形被锁得越死（应变与弹性能都大幅下降）—— 这就是抗剪切；
  κ_s=1000 时"最大应变"反而升高，是因为对角边成了最软的一环。
- **性能代价 +21 %**（L 的 nnz 23496 → 32616，+39 % ⇒ 因子填充上升 ⇒ 回代桶 +17 %；
  投影/散射 +50 %、解包+判据 +31 %）。**这是"多一类约束"的真实价格**，弯曲约束会更贵。
- 验收：`pd_check` 8/8、**306 断言**（新增 4 条剪切构造断言：默认边数不变、每条四边形一条对角边、
  总数、无重复边）、`pd_trans`/`pd_chain`/`pd_diraudit` 全过。

---

## 12. 线性（中点）弯曲约束：可选，已完成 2026-09-22

### 12.1 约束形式与"为什么选这个形式"

沿网格行/列取**连续三个顶点** `(a, b, c)`（b 在中间），

```
A_c x = x_a - 2 x_b + x_c          能量  E_c(x) = (k/2)·‖A_c x‖²
```

等价说法：在**顶点 b 与两邻居的中点之间挂一根静止长度为 0 的弹簧**。它是离散化的
欧拉–伯努利弯曲能（`A_c x` 是曲率的离散代理）。

**选它而不是二面角弯曲（`docs/plan.md` §2.2.5 那种形式）的唯一理由**：它让下面四条性质全部保住：

| 性质 | 中点形式 | 二面角形式 |
|---|---|---|
| `A_c` 是常数矩阵 ⇒ L 与位形无关 ⇒ **只分解一次** | ✅ | ❌（∇θ 依赖位形，每帧重算） |
| 每块是 `k·w_p·w_q·I₃` ⇒ `L = Ã ⊗ I₃` ⇒ **三分量并行保住** | ✅ | ❌ |
| 流形是 `{x : A_c x = 0}`（线性子空间）⇒ 投影 `p_c ≡ 0` ⇒ **局部步无事可做、散射/着色不改** | ✅ | ❌（投影非零） |
| 右端只多一个**与位形无关的常向量**（仅当 stencil 含 pinned 顶点时非零） | ✅ | ❌（每迭代变） |

代价是**约束更密**：每个内部顶点最多进入 4 条 stencil（行 2 + 列 2），而 4-邻域距离
约束只有 4 条边 —— 但它们要耦合**隔一个**的顶点（步长 2），所以填充比距离约束大得多（见 §12.4）。

### 12.2 实现要点（改哪几个文件、为什么）

| 文件 | 改动 |
|---|---|
| `core/mesh/Mesh.{h,cpp}` | 新增 `struct BendStencil {a,b,c,stiffness}` 与 `Mesh::bends`；`addBendingStencils(nx,ny,k)` 沿行/列生成（`(nx-2)*ny + (ny-2)*nx` 条，**端点不生成** = 自由端边界）；函数体**自动重建稀疏结构与关联表**（照抄 `addShearDiagonals`，漏了会越界读）；`buildSparsityPattern()` 把每个 stencil 的 9 个块位置加进 `blocks_`（**必须放行包含 pinned 列的块**，否则 `setFromTriplets` 会把那些值丢掉） |
| `core/mesh/ConstraintColoring.{h,cpp}` | 新增 `BendAdjacency`（顶点→stencil 的 CSR，带 `w_v ∈ {1,-2,1}`）与 `buildBendAdjacency()`，由 `buildSparsityPattern()` 一并构建 |
| `core/assemble/Assembler.{h,cpp}` | `assembleLeftHandSide` 增加弯曲块装配 + 可选出参 `bendingRhs`（默认 nullptr ⇒ 现有调用方行为逐字不变）；新增 `assembleBendingRhs()`；`computeStamp` 把**弯曲刚度**哈希进去（漏了 = 改 k 不重分解，错的物理结果）；`computeStructureStamp` 把 stencil 顶点三元组哈希进去；`Stats::factorNnz`（因子 nnz，诊断用） |
| `core/energy/DistanceTerm.{h,cpp}` | 两条散射入口各加可空 `extraRhs`：种子趟写成 `b[i] = base[i] + (extraRhs ? extraRhs[i] : 0)`。默认 nullptr ⇒ 逐字不变；**不需要任何额外 barrier** |
| `core/sim/Scene.{h,cpp}` | `SceneConfig::bendStiffness`（默认 0）+ 环境变量 `PD_BEND=<k>` 覆盖（照抄 `PD_SHEAR` 的位置与写法）；`SimContext::bendRhs`；打印 `[scene] 弯曲约束：新增 stencil N 条（刚度 …）` |
| `core/sim/Integrator.cpp` | 组装/分解那一趟同时算常向量；**每子步重算一次常向量**（它依赖 `pinPositions`，而拖拽把手只改 b 不改 L —— docs/plan.md §2.2.1 的约定）；融合的投影+散射把 `bendRhs` 传进种子趟；**残差两条口径都加弯曲力**（见 §12.3）；`surrogateEnergy` 加上 `(k/2)‖A_c x‖²` |
| `tests/primitives/test_distance_term.cpp` | 新增 4 个测试（+44 断言：136 → **180**） |
| `_verify/bend_audit.cpp`（`pd_bendaudit`） | 新诊断程序：求解器自检 / 三条链解析解 / 平直零力 / 有限差分梯度 / 跨分量填充 |

### 12.3 ⚠️ 残差口径（本次最容易漏、漏了就是假阴性）

残差是**唯一放行判据**（`residualTolerance > 0` 时位移判据完全不参与判定），所以
"残差里少算一项力"会**直接改变哪些子步被放行**：少了弯曲项 ⇒ 一个明显还受弯的子步会被
判"已收敛" ⇒ **假阴性**（仓库的立场：假阴性比假阳性更危险，见 `docs/perf.md` §7.1）。

所以两条残差口径（串行 `nonlinearResidual` 与逐顶点 gather `vertexResidual`）都加了弯曲力：

```
f_v = Σ_{stencil 含 v}  -k·w_v·(x_a - 2 x_b + x_c)
```

实现上**两条路径共用同一个 `bendForceAt()`**（不是"两处各写一份、靠两张 CSR 顺序一致去论证"），
所以"逐位相同"是**构造性**的。`PD_DEBUG_RESIDUAL=1` 的两口径自查启用弯曲后仍是 0 次告警。

### 12.4 前后对照（同一轮会话内交替 5 次取最小值，vcomp，4 线程，`build/_perf/ab_bend.ps1`）

工况：**40×40 / iters 40 / 300 子步 / 4 线程 / `PD_BEND=1000`**

| 项 | 关闭 | 开启 | 变化 |
|---|---|---|---|
| **单子步**（每子步 ms） | 2.57 ms | **4.45 ms** | **+73.4 %** |
| 预测（累计 ms） | 1.156 ms | 1.314 ms | +13.7 % |
| 投影+散射（累计） | 83.902 ms | 87.848 ms | **+4.7 %**（几乎不变 —— 弯曲没有局部步/散射） |
| 组装+分解（累计） | 3.661 ms | 8.862 ms | +142.1 %（一次性） |
| **全局回代（累计）** | 497.282 ms | **982.591 ms** | **+97.6 %** |
| 解包+判据（累计） | 167.665 ms | 233.209 ms | +39.1 %（残差里多了弯曲力） |
| 速度更新（累计） | 0.221 ms | 0.239 ms | +8.1 % |
| 其它（累计） | 14.731 ms | 18.436 ms | +25.2 % |
| **L 的 nnz** | 23,496 | **41,712** | **+77.6 %** |
| **因子 nnz** | 66,027 | **182,613** | **+176.6 %** |

> **单位口径（2026-09-23 修正）**：`pd_bench` 的阶段表是**300 子步的累计 ms**，
> 而"单子步"只能从结果行的 `单子步 x ms` 取。当时 `ab_bend.ps1` 的解析写成了
> "在子步数行里取第一个 x.y"，抓到的是 `总耗时 0.802 s` 里的 **秒数 0.802**，
> 于是那一列一直被当成 ms 记（差 1000/300 = **3.33 倍**；比值/百分比完全不受影响）。
> 已修脚本，并把本节与 §13 的绝对值按 ×3.33 换算过来。自检方法：**各阶段累计 ms 之和
> 应等于"那个数 × 1000"**（本节：768.6 ≈ 0.770×1000 ✓）。

各轮原始数据（单子步 ms，由当时日志的墙钟秒数 ×3.33 换算）：off 2.607 / 2.577 / 2.603 /
2.567 / 2.583，on 4.540 / 4.500 / 4.450 / 4.463 / 4.463 ⇒ 每轮都是 +71…+75 %，结论稳定。

**代价的诚实结论**：

1. **+73 % 是"多一类约束"的真实价格**，而且**几乎全在回代上**（+97.6 %，
   占总增量的 ~81 %）。原因很直接：弯曲把"隔一个顶点"也耦合起来，**因子填充从 66k 涨到 183k（2.77×）**，
   而回代是延迟受限的（达成的带宽只有流式读写的 9–13 %，`docs/solver-feasibility.md` §2），
   读的因子翻倍时间就跟着涨。**L 的 nnz 只 +78 %，因子却 +177 %** —— 说明代价主要来自消元填充，
   不是矩阵本身的非零数。
2. **投影+散射只 +4.7 %**：弯曲**没有**局部步、**没有**散射，这是选这个形式换来的结构性收益
   （对照剪切约束：$\kappa_s=1e4$ 时投影/散射 +50 %、总代价 +21 %）。
3. **解包+判据 +39 %**：残差里多算一项力（O(3B) 次乘加 + 每顶点一次 gather）。
   这是"口径正确"的必要代价，不接受"为了快而少算"。
4. **组装+分解 +142 %**（一次性，只在 stamp 变化时发生；本工况 300 子步只发生 1 次，
   摊到单子步里只有 0.017 ms）。
5. **物理效果**（同工况，末值）：最终应变 max **0.0586 → 0.0499**、弹性能 0.322 → 0.423
   （弯曲能计入）。末次残差 8.64 → 5235 m/s² —— 后者**不是数值变坏**，而是
   "不平衡力的口径现在含弯曲力、而 40×40/iters40 这个工况本来就没收敛"（收敛 0/300），
   加上 κ_b 在这个间距下折算极硬（见 §12.6 的局限 1）。

   **κ_b 的因果扫描**（同工况，300 子步，4 线程；用来确认"刚度越大变形越小"这条路是通的）：

   | `PD_BEND` | 末次残差 m/s² | 最终应变 max | 平均 | 弹性能 |
   |---|---|---|---|---|
   | 0（关闭） | 8.63991 | 0.0585735 | 0.0013502 | 0.322049 |
   | 0.001 | 8.62434 | 0.0585737 | 0.0013502 | 0.322056 |
   | 1 | 50.33 | 0.05872 | 0.00135012 | 0.326154 |
   | 100 | 2153.4 | 0.06213 | 0.00143839 | 0.37633 |
   | 1000 | 5235.6 | **0.04993** | 0.00139981 | 0.42291 |

   读法：κ_b 极小时应变与"关闭"几乎逐位相同（**这是端点处的自检**：约束趋 0 时行为必须回到基线）；
   随 κ_b 增大，**变形被限制住**（应变 max 0.0586 → 0.0499）、弯曲能上升（0.322 → 0.423）。
   残差单调上升是**预期**的：它是"本子步的不平衡力"，而弯曲力 ∝ κ_b；这个工况的 40 次迭代
   从来没收敛过（收敛 0/300），所以残差读数是"铰接的弯曲力有多大"，不是"解得多差"。
   它的绝对值也提示**这个 κ_b 档位与 40×40/间距 0.025 不匹配**（见 §12.6 局限 1），
   换网格必须重新标定 —— 这也是它默认关闭的另一个理由。

**若代价仍嫌大，两个候选（§13 已实测，一个有效、一个被否）**：

- **只加一个方向**（只行或只列）：✅ **有效**。约束数减半、因子 nnz −49 %、单子步 +15.9 %
  （∓ 全采样 +75.0 %）；代价是抗弯变成**各向异性**（沿网格方向挺、垂直方向软）。见 §13.4。
- **隔点加密**（每两条 stencil 留一条）：❌ **被否**，而且否得比"效果差一点"严重得多 ——
  它是**退化的**：付 +11.5 % 的代价、却几乎不产生任何物理效果（k 加 100 倍，应变只变 0.4 %）。
  原因是没被采样的中心顶点可以自由吸收全部曲率（§13.3 有几何证明 + 反例断言 + 截图）。
- **增大间距**要重标定：见 §12.6 局限 1（等效抗弯刚度 ∝ s⁴/k）。

### 12.5 正确性与确定性（验收）

| 检查 | 结果 |
|---|---|
| `pd_check` 8/8 | 全过 |
| `test_primitives` / `test_spring_vertical` / `test_convergence_criterion` | **350 断言**（primitives 由 136 增至 180，+44 条弯曲断言），0 失败。§13 又加了采样变体断言 ⇒ 现为 **233 + 155 + 15 = 403** |
| `pd_trans` / `pd_chain` / `pd_diraudit` | 全过 |
| `pd_bendaudit`（新） | 全部正确（12 项） |
| **默认关闭（`PD_BEND` 未设）与 `build/_baseline/pd_bench_pre_fuse.exe` 逐字比对** | **True**（收敛统计、末次残差 8.63991 m/s²、最终应变 0.0585735/0.0013502、全程最大应变 0.0652267、弹性能 0.322049、总能量 −10.5941 全部逐字相同） |
| **启用弯曲时跨线程数逐字一致**（1/2/3/4/5/8/18 线程，40×40/iters20/60 子步/`PD_BEND=1000`） | **7/7 全部逐字相同** |
| `pd_solvecomp --grid 40 40 --reps 50 --threads 4`（**默认约束集**） | 跨分量 i≠j 项 **0**；"三分量拆 vs 整趟"最大逐元素差 **0.000e+00** |
| `pd_bendaudit` 的结构自检（**开弯曲** 20×20 网格） | 跨 x/y/z 分量的填充 **0** ⇒ `L = Ã ⊗ I₃` 保住 |
| **采样变体的结构断言**（§13） | 逐块验"每个 3×3 块 = 标量 × I₃"，7 个变体全过；默认选项与不传选项**按序逐条相同** |
| **采样变体的逐线程决定性**（§13.5） | `rows/1`、`checker/1`、`standard/2` 各 7/7（1/2/3/4/5/8/18 线程逐位相同） |

### 12.6 局限（都要如实写进文档，不要粉饰）

1. **弯曲刚度是网格相关的：等效抗弯刚度 ∝ s⁴ / k**（s = 间距）。理由是 `‖A_c x‖²` 里有两个位移差分，
   量纲上不是"曲率²"而是"长度²" ⇒ 它乘上 k 之后仍带长度⁴ 因子。后果：
   **换间距/分辨率必须重新标定 k**，而且它把"弯曲刚度"与"间距"耦合成"同一个 k 在不同网格上手感不同"。
   要尺度无关得改成 `k·s⁴`（或按质量参数化，与 κ 的处理原则一致）。**这一步没有做。**
2. **边界是自由端**：端点不生成 stencil（中心差分需要两侧邻居）⇒ 布料的**边缘一圈不会自己挺起来**。
   标准做法（自然边界条件），但要"夹住边界"（如悬垂布的顶边）得另加约束或把单侧差分加进来。
3. **不能表达目标曲率/预折痕**：能量是 `(k/2)‖A_c x‖²`，静止曲率**恒为 0** ——
   只能表达"想变平"，不能表达"想保持某个折角"。要做预折痕得改成 `(k/2)‖A_c x - d̄_c‖²`，
   那是另一种约束（静止曲率非零），本次没做。
4. **代价 +73 %**（见 §12.4），**因此默认关闭**（`SceneConfig::bendStiffness = 0`）。
   这是刻意的取舍：默认约束集保持最小（`docs/plan.md` 决策 D1），历史基线与断言不受影响。
5. **本轮的实现坑（已修，写在这里提醒后人）**：弯曲的装配规则**不能照抄距离约束的
   "跳过含 pinned 的块"**。被 pin 的行被覆盖为 (对角 1, 右端 q)，这套消元法只在
   **pinned 列在所有自由行里都是 0** 时才自洽 —— 否则 Lᵀ 回代会用那个耦合把 x_pin 从
   `b_pin = q` 上"顶"回别的值，pinned 顶点就不再严格等于把手位置。实测症状很隐蔽：
   三顶点小链上矩阵变成**不定**（行列式变负），**求解器不报错**、给出谁都不满足的解
   （x_pin = −0.924 而 pin 行要求 0），线性残差 8.9e-2；而 Eigen 自带的 `SimplicialLDLT`
   给出**完全相同**的错误解 ⇒ 说明错在系统、不在求解器。pinned 那边的贡献改走右端常向量
   （`assembleBendingRhs` 的 `-k·w_p·C`），两边必须成对。

### 12.7 复现命令

```powershell
# 开启弯曲（默认关闭；两种方式等价）
$env:PD_BEND='1000'
.\build\Release\pd_bench.exe --grid 40 40 --steps 300 --iters 40 --residual-tol 0.3 --threads 4
Remove-Item Env:\PD_BEND

# 弯曲约束的代数审计（左端块 / 右端常向量 / pin 覆盖 / 残差口径 / ⊗ 结构）
.\build\Release\pd_bendaudit.exe

# 开/关成对 A/B（同轮交替、重复 5 次取最小值；日志写 build\_baseline\bend_ab.log）
.\build\_perf\ab_bend.ps1 -Reps 5
```

---

## 13. 弯曲 stencil 的采样变体：一个真降本、一个是退化的（2026-09-22）

§12.4 结尾留了两个"没实测的候选"（只加一个方向、隔点加密）。本轮把它们都实现了并实测，
结论是**一个可以用、另一个必须否掉** —— 而且否掉的理由不是"效果差点"，是**约束退化**。

### 13.1 做法：把"取哪些 stencil"变成显式参数

`Mesh::addBendingStencils(nx, ny, k, opt)` 新增 `BendGenOptions`（`core/mesh/Mesh.h`）：

| 字段 | 取值 | 含义 | stencil 数（40×40） |
|---|---|---|---|
| `sampling` | `Standard` | 行、列都取，中心全取（**默认 = 今天的行为**） | 3040 |
| | `RowsOnly` | 只取行方向（沿 i 的连续三个顶点） | 1520 |
| | `ColsOnly` | 只取列方向（沿 j 的连续三个顶点） | 1520 |
| | `Checkerboard` | 每个中心**只取一个方向**，`(i+j)` 的奇偶决定取行还是取列 | 1520 |
| `stride` | `1`（默认）/`2`/`3` | 中心顶点的采样步长：2 = 隔一个取一个 | 1520 / 1040 |

接线：`SceneConfig::bendSampling` / `bendStride` + 环境变量 `PD_BEND_SAMPLING` / `PD_BEND_STRIDE`
（认不出的取值只打警告、**保留原值**，绝不静默改成别的配置）。默认值下生成的 stencil 与
改动前**按序逐条相同**（有断言钉住：生成次序是残差逐位可复现的前提）。

为什么"少取 stencil"能降本：弯曲的代价主要来自**消元填充**（§12.4），而填充由"每个未知量被
多少约束耦合"决定 —— 弯曲 stencil 的耦合半径是 2，少取就直接少填充。它**不碰任何架构性质**：
stencil 变少 ⇒ `L = Ã ⊗ I₃`、只分解一次、三分量并行回代都自动成立（本轮加了逐块断言，见 §13.5）。

### 13.2 同一轮会话交替 5 次取最小值（`build/_perf/ab_bend_variants.ps1`）

工况：**40×40 / iters 40 / 300 子步 / 4 线程 / κ = 1e4**，`PD_BEND` 见下表。
（"单子步"= 每子步 ms；其余阶段列 = 300 子步累计 ms。见 §12.4 的单位口径说明。）

| 配置 | 刚度 | stencil | L nnz | 因子 nnz | 单子步 | vs off | 最终应变 max | 弹性能 |
|---|---|---|---|---|---|---|---|---|
| off | — | 0 | 23,496 | 66,027 | 2.63 ms | — | 0.05857 | 0.3220 |
| **std/s1（今天）** | 1000 | 3040 | 41,712 | 182,613 | **4.60 ms** | **+75.0 %** | 0.04993 | 0.4229 |
| std/s2（隔行） | 1000 | 1520 | 32,610 | 84,405 | 2.93 ms | +11.5 % | 0.05780 | 0.3163 |
| std/s3（隔行） | 1000 | 1040 | 29,730 | 74,667 | 2.83 ms | +7.7 % | 0.05780 | 0.3164 |
| std/s2 ×2k | 2000 | 1520 | 32,610 | 84,405 | 2.91 ms | +10.8 % | 0.05776 | 0.3161 |
| std/s3 ×3k | 3000 | 1040 | 29,730 | 74,667 | 2.78 ms | +5.8 % | 0.05775 | 0.3161 |
| **rows/s1（单向）** | 1000 | 1520 | 32,604 | **94,077** | 3.04 ms | **+15.9 %** | 0.04351 | 0.4352 |
| rows/s1 ×2k | 2000 | 1520 | 32,604 | 94,077 | 3.07 ms | +16.9 % | 0.03314 | 0.4617 |
| cols/s1（单向） | 1000 | 1520 | 32,604 | 105,954 | 3.28 ms | +24.9 % | 0.06669 | 0.3648 |
| checker/s1（棋盘） | 1000 | 1520 | 32,604 | 112,428 | 3.45 ms | +31.2 % | 0.06429 | 0.4117 |
| checker/s2 | 1000 | 760 | 28,050 | 77,238 | 2.87 ms | +9.3 % | 0.05817 | 0.3170 |

三个立即能读出来的事实：

1. **填充与 stencil 数不是线性关系**：stencil 减半（1520）时因子 nnz 只降到 84k–112k，
   而不是 91k 的一半 —— **同样的 L nnz（32,604）在不同取法下因子 nnz 差 20 %**（84,405 vs 112,428）。
   也就是说"取哪些"比"取多少"更影响填充（AMD 排序吃结构，不吃数量）。
2. **std/s2、std/s3 的应变与弹性能几乎等于"关闭"**（0.05780/0.05857、0.3163/0.3220），
   而且把它们各自 ×2k、×3k 几乎没有变化 ⇒ 这两个配置**白花钱**。见 §13.3。
3. **rows/s1 用 1/4 的代价增量拿到了比全采样更强的应变压制**（0.04351 vs 0.04993，+15.9 % vs +75.0 %）。
   但这不是"更便宜的同一个弯曲"，而是**换了一种材料**（各向异性），见 §13.4。

> 口径提醒：本节 A/B 工况是 **iters 40 且 0/300 子步达到判据**（没收敛），所以"应变/能量"只在
> "同一迭代数下的横向对比"意义上成立。§13.3 另外做了**收敛对照**（iters 400）来支撑结论。

### 13.3 ⚠️ 隔行采样（stride ≥ 2）是退化的 —— 付了代价，几乎没有物理效果

**收敛对照**（40×40 / **iters 400** / 100 子步 / 4 线程；无一达到 0.3 判据，但迭代数相同、可比）：

| 配置 | k = 1e3 应变 max | k = 1e5 应变 max | 刚度 100 倍的效果 |
|---|---|---|---|
| off | 0.06007 | — | — |
| std/s1 | 0.05274 | **0.01290** | **−75.5 %** |
| **std/s2（隔行）** | 0.05941 | **0.05919** | **−0.4 %（≈ 0）** |
| rows/s1（单向） | 0.04290 | 0.01937 | −54.9 % |
| checker/s1（棋盘） | — | 0.02785 | 有效，但只剩约一半 |

**截图证据**（`pd_viewer --grid 60 --stiffness 500 --pin-corners --frames 300`，HUD 上直接印应变/耗时）：

| 配置 | HUD 应变 max | 物理 ms/帧 | 观感 |
|---|---|---|---|
| off | 0.8563 | **0.56** | 深 U 形，底部圆 |
| std/s1 k=1000 | 0.5867 | 24.62 | 底部被拉平、整体绷紧（明显不同） |
| **std/s2 k=1000** | **0.8566** | **14.25** | **与 off 逐像素级相同** |
| rows/s1 k=1000 | 0.7311 | 14.92 | 介于两者之间 |

（截图：`build/_perf/shots/bendvar_{off,std1_1000,std2_1000,rows1_1000,checker1_1000}.png`）

**为什么**（几何/代数，不是"效果弱一点"）：一条 stencil 在中心顶点上要求**局部共线**
`x_i = (x_{i-1} + x_{i+1})/2`。只在**奇数**中心采样时，**偶数顶点那一层子格子完全不受约束**：
把奇数顶点取成两侧偶数顶点的中点，则所有被采样约束的 `A_c x` **恰好为 0**，而偶数子格子可以
任意强弯曲 —— **弯曲自由度整体搬到了没被采样的子格子上**。所以：

> 弯曲这种"每个点上的局部二阶差分"约束，**不能靠抽样降本**（抽样会把自由度漏到未采样的
> 子格子上）；只能靠**减维度**（少一个方向）降本。

这条已钉成断言 `stridedBendingSamplingCannotSeeCoarseBendingSoItIsRejected`
（`tests/primitives/test_distance_term.cpp`）：显式构造一个"中点偏离弦 4c"的强弯曲位形，
断言被采样的 `A_c x` **严格全为 0**（坐标取二进制精确值，所以是 `== 0` 而不是阈值），
同时断言同一位置在**完整采样**下 `A_c x = c ≠ 0`（证明这条断言有分辨力、且完整采样没有这个漏洞）。

**代码里怎么处理**：开关保留（做对照实验要用），但 `stride >= 2` 时运行时会打一行警告
（"实测对弯曲几乎无效、代价却照付"），**只警告不改行为** —— 免得有人静默地跑一个又贵又没用的配置。

### 13.4 单向（rows / cols）是唯一"既真降本又真有效"的变体

- **代价**：stencil 1520（−50 %）、L nnz 32,604（−22 %）、**因子 nnz 94,077（−49 %）**、
  单子步 **+15.9 %**（对照全采样 +75.0 %）。也就是说**把弯曲的代价压到了原来的 1/4 量级**
  （增量 **0.42 ms** vs 1.97 ms，基准 2.63 ms）。
- **物理**：这是**各向异性**材料 —— `rows` 只让"沿网格行/横向的纤维"抗弯，纵向纤维完全没有抗弯。
  它是**换材料，不是"更便宜的同一材料"**，所以不能说"rows 比 std 更好"。
- **在本工况这个指标上更强**：应变 max 0.04351（k=1000）/ 0.03314（k=2000），低于全采样 k=1000 的
  0.04993 —— 因为"最终应变 max"这个标量受横向纤维主导，而 `rows` 恰好把横向拧紧了。
  要注意这个指标**不是对称的**：`cols/s1`（另一个单向）应变 0.06669，比"关闭"还高一点，
  代价 +24.9 % —— 同一"减半"的取法换一个方向就既更贵又更弱。
- **为什么 rows 和 cols 的 L nnz 一样（32,604）而因子 nnz 不同（94,077 vs 105,954）**：
  AMD 排序对结构敏感；横向耦合和纵向耦合在消元顺序下的填充不一样。这也是 §13.2 事实 1 的来源。

**棋盘（checker）不推荐**：它和单向同为 1520 条，但**更贵（+31.2 % vs +15.9 %）且更弱**
（k=1e5 时 0.02785 vs 0.01937）—— 因为它每个方向只覆盖一半中心，**仍然留着未采样的中心
可以吸收曲率**（只是没有 stride 2 那么彻底），等于"两边都不彻底"。

### 13.5 验收与建议

| 检查 | 结果 |
|---|---|
| `pd_check` / `pd_bendaudit` / `pd_trans` / `pd_chain` / `pd_diraudit` / `pd_solvecomp` | 全过（`pd_bendaudit` 12/12；`pd_solvecomp` 跨分量 i≠j = 0、拆分 vs 整趟逐位差 0.000e+00） |
| 断言总数 | **233 + 155 + 15 = 403**，0 失败（primitives 由 180 增至 233，+53：采样变体 + 退化反例） |
| 默认关闭 vs `pd_bench_pre_fuse.exe` | **逐字相同**（残差 89.3655、应变 0.0640233/0.0018079、全程峰值 0.0652267、弹性能 0.419269、总能量 −6.30461） |
| 采样变体的逐线程决定性 | `rows/1`、`checker/1`、`standard/2` 在 1/2/3/4/5/8/18 线程下各 **7/7 逐位相同** |
| 采样变体的 ⊗ 结构 | 7 个变体逐块验"3×3 块 = 标量 × I₃"全过 |

**建议**（默认值一律不动：`bendStiffness = 0`）：

1. 要开弯曲就用 **`std/s1`**（各向同性、真正的弯曲刚度）。
2. **确实要降本**就用 **`rows/s1` 或 `cols/s1`**（因子 nnz −49 %、单子步 +15.9 %），
   代价是**各向异性**：必须按"换了一种材料"去标定刚度，不能指望它复现各向同性的观感。
3. **`checker` 不推荐**（同规模下更贵更弱）；**`stride ≥ 2` 禁用**（退化，见 §13.3）。
4. 没做的：`k` 的自动标定（§12.6 局限 1 仍在：等效抗弯刚度 ∝ s⁴/k，换网格要重标定）、
   以及"单向 + 各向异性标定"的正式工作流。

### 13.6 复现命令

```powershell
# 采样变体的降本对照（同一轮会话交替 5 次取最小值；日志 build\_baseline\bend_variants_ab.log）
.\build\_perf\ab_bend_variants.ps1 -Reps 5

# 单点：只取行方向的完整采样（k=2000 补回一半密度）
$env:PD_BEND='2000'; $env:PD_BEND_SAMPLING='rows'; $env:PD_BEND_STRIDE='1'
.\build\Release\pd_bench.exe --grid 40 40 --steps 300 --iters 40 --residual-tol 0.3 --threads 4
Remove-Item Env:\PD_BEND,Env:\PD_BEND_SAMPLING,Env:\PD_BEND_STRIDE

# 退化证据：k 加 100 倍、隔行采样的应变几乎不变（对照 std/s1）
$env:PD_BEND='100000'; $env:PD_BEND_SAMPLING='standard'; $env:PD_BEND_STRIDE='2'
.\build\Release\pd_bench.exe --grid 40 40 --steps 100 --iters 400 --residual-tol 0.3 --threads 4
Remove-Item Env:\PD_BEND,Env:\PD_BEND_SAMPLING,Env:\PD_BEND_STRIDE

# 截图对照（HUD 上直接印应变与 ms/帧）
.\build\Release\pd_viewer.exe --grid 60 --stiffness 500 --pin-corners --frames 300 --damping 0.05 `
  --shot build\_perf\shots\bendvar_off.png
```

---

## 14. 惯性右端并行化：实测无收益（已否，2026-09-23）

**动机**：一个子步里惯性右端 `bBase = (M/h²)x̂` 原本是 `omp single`（一条线程干、其余三条
在 barrier 上空转等），是区域内少数几个"单线程"阶段之一。一个自然的问题是：把它改成
`omp for`（按顶点切给所有线程）能不能赚到一点？

**做法**：`Assembler` 新增 `assembleInertialRhsInRegion()`（函数体内 `#pragma omp for`，
与 `DistanceTerm::projectAndScatterIntoInRegion` 同一约定），两条路径共用同一个
`inertialRhsVertex()` 算术 ⇒ **逐位相同是构造性的**；`Integrator` 把那一步从 `single` 换成
它。正确性零风险：逐顶点只写自己的 `b[3v..3v+2]`，**没有归并、没有求和顺序变化**；
同步点数也不变（原来靠 `single` 的隐式 barrier，现在靠 `for` 的隐式 barrier，两者的位置
正好就是"预测已完成"与"bBase 已写完"所必需的那两个）。

**实测**（`build/_perf/ab_inertial.ps1`，同轮交替 7 次取最小，40×40 / iters 40 / 300 子步）
——注意这里用了**三方**：旧 single 二进制、新 `for` 二进制、以及**控制组**（同一份新源码
把 `#if` 关掉、即走 single 路径再构建一次）：

| 线程 | 旧-single | 控制-single（同源码另一次构建） | 新-for | 新 vs 旧 | 新 vs 控制 |
|---|---|---|---|---|---|
| **1** | 5.494 ms | 5.482 ms | **5.389 ms** | −1.9 % | **−1.7 %** |
| 4（默认） | 2.629 ms | 2.621 ms | 2.589 ms | −1.5 % | −1.2 % |
| 18 | 3.644 ms | 3.594 ms | 3.573 ms | −1.9 % | −0.6 % |

（同源码两次构建的差：+0.2 % / +0.3 % / +1.4 % ⇒ 这是**构建/噪声地板**。）

**结论：否。** 两条独立证据：

1. **1 线程那行是决定性的**：单线程时 `omp for` 与串行循环是同一件事，理论差值必须是 0，
   可它已经"快了" 1.7 % ⇒ 这点差别**不可能来自并行**，来自代码生成/布局（顺带还去掉了
   每子步一次的 `b.resize()`）。换句话说，**真正的并行收益要用"新 vs 控制"去看**，
   而那两列在 4T/18T 只剩 −1.2 % / −0.6 % —— 落在地板里。
2. **量级本来就不够**：这一步每子步只跑一次、是 O(n)=1600 个顶点的纯读+写（串行约 3–5 µs），
   而 4 线程下子步是 2630 µs ⇒ 即使并行**无限快**，上限也只有 **0.1 %** 量级。
   要让"区域里没有单线程阶段"成为收益，得先有一个够贵的单线程阶段。

**方法论收获（比结论本身更值钱）**：这就是 `docs/perf.md` §0 纪律 2 与 pit #3 的又一个实例 ——
**"改了代码之后变快了" ≠ "改动的机制起作用了"**。凡是"把某一步并行化/换写法"的对照，
必须至少有一个**让该机制失效的档位**（这里是 1 线程）作为对照；否则 1–2 % 的代码布局抖动
会被当成并行收益写进文档。本项目的 pit #3（热路径里加 O(1) 校验就慢 28 %）是同一个坑的
另一种形态。

**代码去向**：实现**已回退**（不留 `#if` 开关、不留死代码）。复现只用
`build/_perf/ab_inertial.ps1` + 把 §14 里那 6 行改回去。

---

## 15. 复现命令

```powershell
cd D:\dsh_workspace\pd_ipc ; .\tools\build.ps1

# 工况 A（本文件的主工况）：1 / 8 / 18 线程
.\build\Release\pd_bench.exe --grid 40 40 --steps 300 --iters 40 --residual-tol 0.3 --threads 1
.\build\Release\pd_bench.exe --grid 40 40 --steps 300 --iters 40 --residual-tol 0.3 --threads 8
.\build\Release\pd_bench.exe --grid 40 40 --steps 300 --iters 40 --residual-tol 0.3 --threads 18

# 规模依赖（大网格上并行收益才明显）
.\build\Release\pd_bench.exe --grid 200 200 --steps 8  --iters 10 --threads 1|2|4|18
.\build\Release\pd_bench.exe --grid 200 200 --steps 100 --iters 4 --threads 1|8|18   # M2 判据相关

# 交互档（60×60 / 2 次迭代，接近查看器稳态）与有界窗口运行
.\build\Release\pd_bench.exe --grid 60 60 --steps 300 --iters 2 --residual-tol 0.3
.\build\Release\pd_viewer.exe --frames 400

# 正确性与确定性（含着色的结构/确定性断言、跨线程位级一致）
.\build\Release\pd_check.exe ; .\build\Release\test_primitives.exe
.\build\Release\test_spring_vertical.exe ; .\build\Release\test_convergence_criterion.exe
.\build\Release\pd_trans.exe ; .\build\Release\pd_chain.exe ; .\build\Release\pd_diraudit.exe

# Phase 3 的两口径自查：PD_DEBUG_RESIDUAL=1 时串行/并行两条残差都算并互相比对（>1e-9 告警）
$env:PD_DEBUG_RESIDUAL='1'
.\build\Release\pd_bench.exe --grid 40 40 --steps 2 --iters 8 --residual-tol 0.3 --threads 4
Remove-Item Env:\PD_DEBUG_RESIDUAL

# Phase 3 的前后对照（同一会话内交替 A/B、取最小值；脚本自己核对两个 exe 的 OpenMP 运行时）
#   ⚠ 该脚本在 build/ 下（被 gitignore），且**必须带 UTF-8 BOM** —— PowerShell 5.1 读无 BOM 的
#     中文 .ps1 会按 GBK 解码而报语法错。改完请复查前 3 字节是 EF BB BF。
.\build\_perf\ab_residual.ps1 -Reps 4        # 日志写到 build\_baseline\phase3_ab.log

# 运行时核对（换过 OpenMP 开关之后必做）：exe 里不应出现 libomp
#   $b=[IO.File]::ReadAllBytes("build\Release\pd_bench.exe"); ([regex]::Matches([Text.Encoding]::ASCII.GetString($b),'libomp')).Count

# 把本文件里的数字画成看板（分函数层级的堆叠条 + 线程扫描 + 成对对照 + Phase 3/4c/4d A/B
#   + §八 剪切 / §九 弯曲 / §十 采样变体 / §十一 惯性右端（已否）+ 查看器时间线）
node build\_perf\make_report.js          # 生成 build\_perf\perf-report.html（自包含，可直接双击打开）
node build\_perf\check_report.js          # 几何 + 新板块自检（用 DOM 桩真跑一遍渲染代码）
node build\_perf\serve.js                # 可选：静态服务，浏览器开 http://127.0.0.1:8137/

# 全局步（回代）的可行性审计：性质 / 层集 / 换排序 / 并行原型（结论见 docs/solver-feasibility.md）
.\build\Release\pd_solveaudit.exe --grid 200 200 --order amd --solves 40 --proto-threads 1,2,4,8

# 回代改造（§8）的前后对照：基线（Eigen solve）/ 变体 A（CSR）/ 变体 B（scatter）交替取最小
.\build\_perf\ab_solve.ps1 -Reps 5          # 日志写到 build\_baseline\solve_ab.log

# Phase 4c（§9）的三分量拆分：结构自检 + 逐位比对 + 区域内交替微基准
.\build\Release\pd_solvecomp.exe --grid  40  40 --reps 200 --threads 4
.\build\Release\pd_solvecomp.exe --grid 100 100 --reps 100 --threads 4
.\build\Release\pd_solvecomp.exe --grid 200 200 --reps  40 --threads 4
#   读法：① "跨分量的 i≠j 项" 必须为 0；② "最大逐元素差" 必须是 0.000e+00（逐位相同）；
#         ③ B/A 就是三分量并行的加速比（上界 3.00×）。

# Phase 4c 的生产 A/B：交替跑改动前存档的二进制与本版（同一会话、各取最小值）
.\build\_baseline\pd_bench_pre_comp.exe --grid 40 40 --steps 300 --iters 40 --residual-tol 0.3 --threads 4
.\build\Release\pd_bench.exe           --grid 40 40 --steps 300 --iters 40 --residual-tol 0.3 --threads 4

# 弯曲约束（§12）的成对 A/B：同一轮会话内交替 5 次取最小值（开/关分别取最小）
.\build\_perf\ab_bend.ps1 -Reps 5 -Bend 1000        # 日志：build\_baseline\bend_ab.log

# 弯曲 stencil 采样变体（§13）的降本对照：off / 全采样 / 单向 / 棋盘 / 隔行 × 刚度补偿
.\build\_perf\ab_bend_variants.ps1 -Reps 5          # 日志：build\_baseline\bend_variants_ab.log

# 惯性右端并行化（§14）的三方 A/B：旧-single / 控制-single / 新-for（7 次交替取最小）
#   ⚠ 源码已回退 ⇒ 现在跑它两端是同一份代码（差值应 ≈ 0）；要重做实验先把 §14 那几行改回去
.\build\_perf\ab_inertial.ps1 -Reps 7               # 日志：build\_baseline\inertial_ab3.log

# Phase 4c 的看板数据刷新（① 线程扫描日志 ② phase4c_ab.log ③ 剪切/弯曲/采样变体的 A/B 日志）
#   → 重生成 → 自检 → 起静态服务
.\build\_perf\refresh_report.ps1            # 约 5 分钟；-SkipSweep 只重跑 A/B；-Reps N 调重复次数
node build\_perf\make_report.js             # 生成 build\_perf\perf-report.html
node build\_perf\check_report.js            # 自检（段数 / 越界 / 标尺文本 + 新板块非空与关键数字反查）
node build\_perf\serve.js                   # http://127.0.0.1:8137/
```

> **看板各板块的数据来源**（改脚本时这几对数不能脱钩）：
> §五/§六/§七 ← `phase3_ab.log` / `phase4c_ab.log` / `phase4d_ab.log`（固定列格式，`parsePhase3`）；
> §八 ← `shear_ab.log`（`ab_shear.ps1`）；§九 ← `bend_ab.log`（`ab_bend.ps1`）；
> §十 ← `bend_variants_ab.log`（`ab_bend_variants.ps1`，"多配置 × 多档"格式，`parseVariants`）；
> §十一 ← `inertial_ab3.log`（三方对照，需 `build/_baseline/pd_bench_ctrl_single.exe` 这个控制组二进制
> —— 用 `#if 0` 把实验开关关掉再构建一次即可；**实验源码已回退**，见 §14）。
> 两张"两值对照"表（§八/§九）由 `parseTwoWay` 解析；**解析口径**：绝对数列一律取
> `单子步 x ms` 字段本身，不要"取行里第一个 x.y"（那是 `总耗时` 的秒数，见 §12.4）。

> **每次触及热路径的改动都要在本文件补"前后对照"**（`docs/contributing.md` §6），
> 并在提交信息里附上同一轮会话的成对数字。
